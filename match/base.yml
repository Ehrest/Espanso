# Automatically generated by espanso migration tool
# Original file: default.yml

matches:
  - trigger: ":ulog"
    replace: "Debug.Log(\"$|$\")"
  - trigger: ":jslog"
    replace: "console.log(\"$|$\")"
  - trigger: ":date"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%Y%m%d"
  - trigger: ":ddate"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%Y.%m.%d"
  - trigger: ":udate"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%Y_%m_%d"
  - trigger: ":sdate"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%d/%m/%Y"
  - trigger: ":uid"
    replace: "{{uid}}"
    vars:
      - name: uid
        type: date
        params:
          format: "%Y%m%d%H%M%S"
  - trigger: ":pts"
    replace: "\\[T]/"
  - trigger: ":ppts"
    replace: "\\\\[T]/"
  - trigger: ":todo"
    replace: "- [ ] "
  - trigger: ":mlink"
    replace: "[$|$]({{clipboard}})"
    vars:
      - name: clipboard
        type: clipboard